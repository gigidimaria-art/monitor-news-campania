services:
  - type: cron
    name: monitor-news-campania
    schedule: "*/10 * * * *"
    runtime: python3
    buildCommand: ""
    startCommand: "python monitor.py"
    envVars:
      - key: DATABASE_URL
        sync: false
